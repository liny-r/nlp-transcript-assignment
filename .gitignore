# Transcripts — large, provided by course
ECT/
ECT.zip

# Price cache — regenerated by yfinance
cache/prices/

# Raw LLM outputs — large, JSON extractions are enough
cache/extractions/*.raw.txt

# System / IDE
.DS_Store
.claude/

# Data files
data/
*.csv
